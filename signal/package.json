{
  "name": "opencraft-signal",
  "private": true,
  "type": "module",
  "description": "Co-op signalling Worker for OpenCraft (Cloudflare Workers + Durable Objects).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.141.0"
  }
}
